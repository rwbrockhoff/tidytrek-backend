version: '3'
services:
  tidytrek-api:
    build:
      target: development
    volumes:
      - /app
    environment:
      - NODE_ENV=development
      - DB_HOST=host.docker.internal
    command: npm run dev
