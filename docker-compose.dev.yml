version: '3'
services:
  tidytrek-api:
    volumes:
      - ./:/app
      # - /app/node_modules
    env_file:
      - ./dev.env
    environment:
      - NODE_ENV=development
      - DB_HOST=host.docker.internal

    command: npm run dev
